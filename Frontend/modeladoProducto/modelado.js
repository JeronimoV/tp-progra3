var imagen = document.getElementsByClassName("imagen");
var nombre = document.getElementsByClassName("nombre");
var precio = document.getElementsByClassName("precio");

function crearProducto(){ //Modificar luego para usarlo en base de datos
    var imagenInput = imagen.value;
    var nombreInput = imagen.value;
    var precioInput = imagen.value;

    //Aca iria la logica de crear en la base de datos
}